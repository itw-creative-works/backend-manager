module.exports = function (assistant) {
  return {
    // DEFAULTS
    ['defaults']: {
      // Common
      delay: {
        types: ['number'],
        default: 1000,
        value: undefined,
      },
    },
  };
}
